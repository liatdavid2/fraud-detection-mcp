from app.sqlite_store import FraudSQLiteStore


def test_safe_select_blocks_writes(tmp_path):
    # Placeholder test template. Requires a real downloaded dataset to instantiate the store.
    assert True
